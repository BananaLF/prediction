#!/usr/bin/env sh

set -eu

repo_dir=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
python_bin="$repo_dir/.venv/bin/python"

if [ ! -x "$python_bin" ]; then
  printf '%s\n' "error: missing .venv/bin/python; create .venv first" >&2
  exit 1
fi

exec "$python_bin" -m predmarket.cli "$@"
